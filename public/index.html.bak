<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <link rel="icon" href="%PUBLIC_URL%/favicon.ico" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <meta name="theme-color" content="#000000" />
    <meta
      name="description"
      content="Web site created using create-react-app"
    />
    <link rel="apple-touch-icon" href="%PUBLIC_URL%/logo192.png" />
    <!--
      manifest.json provides metadata used when your web app is installed on a
      user's mobile device or desktop. See https://developers.google.com/web/fundamentals/web-app-manifest/
    -->
    <link rel="manifest" href="%PUBLIC_URL%/manifest.json" />
    <!--
      Notice the use of %PUBLIC_URL% in the tags above.
      It will be replaced with the URL of the `public` folder during the build.
      Only files inside the `public` folder can be referenced from the HTML.

      Unlike "/favicon.ico" or "favicon.ico", "%PUBLIC_URL%/favicon.ico" will
      work correctly both with client-side routing and a non-root public URL.
      Learn how to configure a non-root public URL by running `npm run build`.
    -->
    <title>Spexture-com</title>
    <style>
      /* Dark mode support for test commands panel */
      @media (prefers-color-scheme: dark) {
        .test-panel {
          background: #2d2d2d !important;
          border-color: #404040 !important;
          color: #e0e0e0 !important;
        }
        
        .test-panel-title {
          color: #ffffff !important;
        }
        
        .test-panel strong {
          color: #ffffff !important;
        }
        
        .test-panel code {
          background: #1a1a1a !important;
          color: #4fc3f7 !important;
          border: 1px solid #404040 !important;
        }
        
        .test-panel .copy-command-btn {
          background: #1976d2 !important;
          color: white !important;
        }
        
        .test-panel .copy-command-btn:hover {
          background: #1565c0 !important;
        }
        
        .close-test-btn {
          color: #b0b0b0 !important;
        }
        
        .close-test-btn:hover {
          color: #ffffff !important;
          background: rgba(255, 255, 255, 0.1) !important;
        }
        
        .test-panel-kbd {
          background: #404040 !important;
          border-color: #606060 !important;
          color: #e0e0e0 !important;
        }
        
        .test-panel-footer {
          border-top-color: #404040 !important;
          color: #b0b0b0 !important;
        }
      }
      
      /* Custom scrollbar styling */
      #commands-scroll-container::-webkit-scrollbar {
        width: 8px;
      }
      
      #commands-scroll-container::-webkit-scrollbar-track {
        background: #f1f1f1;
        border-radius: 4px;
      }
      
      #commands-scroll-container::-webkit-scrollbar-thumb {
        background: #888;
        border-radius: 4px;
      }
      
      #commands-scroll-container::-webkit-scrollbar-thumb:hover {
        background: #555;
      }
      
      @media (prefers-color-scheme: dark) {
        #commands-scroll-container::-webkit-scrollbar-track {
          background: #2d2d2d;
        }
        
        #commands-scroll-container::-webkit-scrollbar-thumb {
          background: #666;
        }
        
        #commands-scroll-container::-webkit-scrollbar-thumb:hover {
          background: #888;
        }
      }
    </style>
  </head>
  <body>
    <noscript>You need to enable JavaScript to run this app.</noscript>
    <div id="root"></div>

      <div style="display: flex; justify-content: space-between; align-items: center; margin-bottom: 10px; flex-shrink: 0;">
        <div class="test-panel-title" style="font-weight: bold; color: #495057; font-size: 14px;">🧪 Developer Commands</div>
        <button id="close-test-panel" class="close-test-btn" style="background: transparent; border: none; font-size: 18px; cursor: pointer; color: #6c757d; padding: 0; width: 24px; height: 24px; line-height: 1;">×</button>
      </div>
      
      <!-- Scrollable content area -->
      <div id="commands-scroll-container" style="overflow-y: auto; overflow-x: hidden; flex: 1; padding-right: 5px; margin-right: -5px;">
        
        <!-- Start Services Section -->
        <div style="margin-bottom: 15px;">
          <div style="font-weight: bold; color: #495057; font-size: 13px; margin-bottom: 8px; padding-bottom: 4px; border-bottom: 1px solid #dee2e6;">🚀 Start Services (Docker)</div>
          
          <div style="margin-bottom: 8px;">
            <div style="display: flex; align-items: center; gap: 8px; margin-top: 4px;">
              <button class="copy-command-btn" data-command="npm run start:services:detached" style="background: #007bff; color: white; border: none; border-radius: 3px; padding: 4px 8px; font-size: 11px; cursor: pointer; white-space: nowrap;">Copy</button>
              <code style="background: #e9ecef; padding: 2px 6px; border-radius: 3px; flex: 1; font-family: 'Courier New', monospace;">npm run start:services:detached</code>
            </div>
          </div>
          
          <div style="margin-bottom: 8px;">
            <div style="display: flex; align-items: center; gap: 8px; margin-top: 4px;">
              <button class="copy-command-btn" data-command="docker compose up -d --build" style="background: #007bff; color: white; border: none; border-radius: 3px; padding: 4px 8px; font-size: 11px; cursor: pointer; white-space: nowrap;">Copy</button>
              <code style="background: #e9ecef; padding: 2px 6px; border-radius: 3px; flex: 1; font-family: 'Courier New', monospace;">docker compose up -d --build</code>
            </div>
          </div>
          
          <div style="margin-bottom: 8px;">
            <div style="display: flex; align-items: center; gap: 8px; margin-top: 4px;">
              <button class="copy-command-btn" data-command="npm run stop:services" style="background: #007bff; color: white; border: none; border-radius: 3px; padding: 4px 8px; font-size: 11px; cursor: pointer; white-space: nowrap;">Copy</button>
              <code style="background: #e9ecef; padding: 2px 6px; border-radius: 3px; flex: 1; font-family: 'Courier New', monospace;">npm run stop:services</code>
            </div>
          </div>
          
          <div style="margin-bottom: 8px;">
            <div style="display: flex; align-items: center; gap: 8px; margin-top: 4px;">
              <button class="copy-command-btn" data-command="npm start" style="background: #007bff; color: white; border: none; border-radius: 3px; padding: 4px 8px; font-size: 11px; cursor: pointer; white-space: nowrap;">Copy</button>
              <code style="background: #e9ecef; padding: 2px 6px; border-radius: 3px; flex: 1; font-family: 'Courier New', monospace;">npm start</code>
            </div>
          </div>
        </div>
        
        <!-- Client Tests Section -->
        <div style="margin-bottom: 15px;">
          <div style="font-weight: bold; color: #495057; font-size: 13px; margin-bottom: 8px; padding-bottom: 4px; border-bottom: 1px solid #dee2e6;">🧪 Client Tests</div>
          
          <div style="margin-bottom: 8px;">
            <div style="display: flex; align-items: center; gap: 8px; margin-top: 4px;">
              <button class="copy-command-btn" data-command="npm test -- --watchAll=false" style="background: #007bff; color: white; border: none; border-radius: 3px; padding: 4px 8px; font-size: 11px; cursor: pointer; white-space: nowrap;">Copy</button>
              <code style="background: #e9ecef; padding: 2px 6px; border-radius: 3px; flex: 1; font-family: 'Courier New', monospace;">npm test -- --watchAll=false</code>
            </div>
          </div>
          
          <div style="margin-bottom: 8px;">
            <div style="display: flex; align-items: center; gap: 8px; margin-top: 4px;">
              <button class="copy-command-btn" data-command="npm run test:integration" style="background: #007bff; color: white; border: none; border-radius: 3px; padding: 4px 8px; font-size: 11px; cursor: pointer; white-space: nowrap;">Copy</button>
              <code style="background: #e9ecef; padding: 2px 6px; border-radius: 3px; flex: 1; font-family: 'Courier New', monospace;">npm run test:integration</code>
            </div>
          </div>
          
          <div style="margin-bottom: 8px;">
            <div style="display: flex; align-items: center; gap: 8px; margin-top: 4px;">
              <button class="copy-command-btn" data-command="npm run test:all" style="background: #007bff; color: white; border: none; border-radius: 3px; padding: 4px 8px; font-size: 11px; cursor: pointer; white-space: nowrap;">Copy</button>
              <code style="background: #e9ecef; padding: 2px 6px; border-radius: 3px; flex: 1; font-family: 'Courier New', monospace;">npm run test:all</code>
            </div>
          </div>
          
          <div style="margin-bottom: 8px;">
            <div style="display: flex; align-items: center; gap: 8px; margin-top: 4px;">
              <button class="copy-command-btn" data-command="npm run test:coverage" style="background: #007bff; color: white; border: none; border-radius: 3px; padding: 4px 8px; font-size: 11px; cursor: pointer; white-space: nowrap;">Copy</button>
              <code style="background: #e9ecef; padding: 2px 6px; border-radius: 3px; flex: 1; font-family: 'Courier New', monospace;">npm run test:coverage</code>
            </div>
          </div>
        </div>
        
        <!-- Server Tests Section -->
        <div style="margin-bottom: 15px;">
          <div style="font-weight: bold; color: #495057; font-size: 13px; margin-bottom: 8px; padding-bottom: 4px; border-bottom: 1px solid #dee2e6;">🔧 Server Tests (Docker DB)</div>
          
          <div style="margin-bottom: 8px;">
            <div style="display: flex; align-items: center; gap: 8px; margin-top: 4px;">
              <button class="copy-command-btn" data-command="cd server && npm test" style="background: #007bff; color: white; border: none; border-radius: 3px; padding: 4px 8px; font-size: 11px; cursor: pointer; white-space: nowrap;">Copy</button>
              <code style="background: #e9ecef; padding: 2px 6px; border-radius: 3px; flex: 1; font-family: 'Courier New', monospace;">cd server && npm test</code>
            </div>
          </div>
          
          <div style="margin-bottom: 8px;">
            <div style="display: flex; align-items: center; gap: 8px; margin-top: 4px;">
              <button class="copy-command-btn" data-command="cd server && npm test -- --coverage" style="background: #007bff; color: white; border: none; border-radius: 3px; padding: 4px 8px; font-size: 11px; cursor: pointer; white-space: nowrap;">Copy</button>
              <code style="background: #e9ecef; padding: 2px 6px; border-radius: 3px; flex: 1; font-family: 'Courier New', monospace;">cd server && npm test -- --coverage</code>
            </div>
          </div>
          
          <div style="margin-bottom: 8px;">
            <div style="display: flex; align-items: center; gap: 8px; margin-top: 4px;">
              <button class="copy-command-btn" data-command="cd server && npm test -- api.test.js" style="background: #007bff; color: white; border: none; border-radius: 3px; padding: 4px 8px; font-size: 11px; cursor: pointer; white-space: nowrap;">Copy</button>
              <code style="background: #e9ecef; padding: 2px 6px; border-radius: 3px; flex: 1; font-family: 'Courier New', monospace;">cd server && npm test -- api.test.js</code>
            </div>
          </div>
          
          <div style="margin-bottom: 8px;">
            <div style="display: flex; align-items: center; gap: 8px; margin-top: 4px;">
              <button class="copy-command-btn" data-command="cd server && npm test -- middleware/auth.test.js" style="background: #007bff; color: white; border: none; border-radius: 3px; padding: 4px 8px; font-size: 11px; cursor: pointer; white-space: nowrap;">Copy</button>
              <code style="background: #e9ecef; padding: 2px 6px; border-radius: 3px; flex: 1; font-family: 'Courier New', monospace;">cd server && npm test -- middleware/auth.test.js</code>
            </div>
          </div>
        </div>
        
        <!-- Database Commands Section -->
        <div style="margin-bottom: 15px;">
          <div style="font-weight: bold; color: #495057; font-size: 13px; margin-bottom: 8px; padding-bottom: 4px; border-bottom: 1px solid #dee2e6;">🗄️ Database (Docker)</div>
          
          <div style="margin-bottom: 8px;">
            <div style="display: flex; align-items: center; gap: 8px; margin-top: 4px;">
              <button class="copy-command-btn" data-command="npm run db:init" style="background: #007bff; color: white; border: none; border-radius: 3px; padding: 4px 8px; font-size: 11px; cursor: pointer; white-space: nowrap;">Copy</button>
              <code style="background: #e9ecef; padding: 2px 6px; border-radius: 3px; flex: 1; font-family: 'Courier New', monospace;">npm run db:init</code>
            </div>
          </div>
          
          <div style="margin-bottom: 8px;">
            <div style="display: flex; align-items: center; gap: 8px; margin-top: 4px;">
              <button class="copy-command-btn" data-command="npm run db:start" style="background: #007bff; color: white; border: none; border-radius: 3px; padding: 4px 8px; font-size: 11px; cursor: pointer; white-space: nowrap;">Copy</button>
              <code style="background: #e9ecef; padding: 2px 6px; border-radius: 3px; flex: 1; font-family: 'Courier New', monospace;">npm run db:start</code>
            </div>
          </div>
          
          <div style="margin-bottom: 8px;">
            <div style="display: flex; align-items: center; gap: 8px; margin-top: 4px;">
              <button class="copy-command-btn" data-command="npm run db:stop" style="background: #007bff; color: white; border: none; border-radius: 3px; padding: 4px 8px; font-size: 11px; cursor: pointer; white-space: nowrap;">Copy</button>
              <code style="background: #e9ecef; padding: 2px 6px; border-radius: 3px; flex: 1; font-family: 'Courier New', monospace;">npm run db:stop</code>
            </div>
          </div>
          
          <div style="margin-bottom: 8px;">
            <div style="display: flex; align-items: center; gap: 8px; margin-top: 4px;">
              <button class="copy-command-btn" data-command="npm run db:shell" style="background: #007bff; color: white; border: none; border-radius: 3px; padding: 4px 8px; font-size: 11px; cursor: pointer; white-space: nowrap;">Copy</button>
              <code style="background: #e9ecef; padding: 2px 6px; border-radius: 3px; flex: 1; font-family: 'Courier New', monospace;">npm run db:shell</code>
            </div>
          </div>
          
          <div style="margin-bottom: 8px;">
            <div style="display: flex; align-items: center; gap: 8px; margin-top: 4px;">
              <button class="copy-command-btn" data-command="npm run db:status" style="background: #007bff; color: white; border: none; border-radius: 3px; padding: 4px 8px; font-size: 11px; cursor: pointer; white-space: nowrap;">Copy</button>
              <code style="background: #e9ecef; padding: 2px 6px; border-radius: 3px; flex: 1; font-family: 'Courier New', monospace;">npm run db:status</code>
            </div>
          </div>
          
          <div style="margin-bottom: 8px;">
            <div style="display: flex; align-items: center; gap: 8px; margin-top: 4px;">
              <button class="copy-command-btn" data-command="npm run db:logs" style="background: #007bff; color: white; border: none; border-radius: 3px; padding: 4px 8px; font-size: 11px; cursor: pointer; white-space: nowrap;">Copy</button>
              <code style="background: #e9ecef; padding: 2px 6px; border-radius: 3px; flex: 1; font-family: 'Courier New', monospace;">npm run db:logs</code>
            </div>
          </div>
          
          <div style="margin-bottom: 8px;">
            <div style="display: flex; align-items: center; gap: 8px; margin-top: 4px;">
              <button class="copy-command-btn" data-command="docker compose ps" style="background: #007bff; color: white; border: none; border-radius: 3px; padding: 4px 8px; font-size: 11px; cursor: pointer; white-space: nowrap;">Copy</button>
              <code style="background: #e9ecef; padding: 2px 6px; border-radius: 3px; flex: 1; font-family: 'Courier New', monospace;">docker compose ps</code>
            </div>
          </div>
        </div>
        
        <!-- API/Run Commands Section -->
        <div style="margin-bottom: 15px;">
          <div style="font-weight: bold; color: #495057; font-size: 13px; margin-bottom: 8px; padding-bottom: 4px; border-bottom: 1px solid #dee2e6;">🔌 API & Run</div>
          
          <div style="margin-bottom: 8px;">
            <div style="display: flex; align-items: center; gap: 8px; margin-top: 4px;">
              <button class="copy-command-btn" data-command="docker compose exec server npm run dev" style="background: #007bff; color: white; border: none; border-radius: 3px; padding: 4px 8px; font-size: 11px; cursor: pointer; white-space: nowrap;">Copy</button>
              <code style="background: #e9ecef; padding: 2px 6px; border-radius: 3px; flex: 1; font-family: 'Courier New', monospace;">docker compose exec server npm run dev</code>
            </div>
          </div>
          
          <div style="margin-bottom: 8px;">
            <div style="display: flex; align-items: center; gap: 8px; margin-top: 4px;">
              <button class="copy-command-btn" data-command="docker compose logs -f server" style="background: #007bff; color: white; border: none; border-radius: 3px; padding: 4px 8px; font-size: 11px; cursor: pointer; white-space: nowrap;">Copy</button>
              <code style="background: #e9ecef; padding: 2px 6px; border-radius: 3px; flex: 1; font-family: 'Courier New', monospace;">docker compose logs -f server</code>
            </div>
          </div>
          
          <div style="margin-bottom: 8px;">
            <div style="display: flex; align-items: center; gap: 8px; margin-top: 4px;">
              <button class="copy-command-btn" data-command="curl http://localhost:3001/health" style="background: #007bff; color: white; border: none; border-radius: 3px; padding: 4px 8px; font-size: 11px; cursor: pointer; white-space: nowrap;">Copy</button>
              <code style="background: #e9ecef; padding: 2px 6px; border-radius: 3px; flex: 1; font-family: 'Courier New', monospace;">curl http://localhost:3001/health</code>
            </div>
          </div>
          
          <div style="margin-bottom: 8px;">
            <div style="display: flex; align-items: center; gap: 8px; margin-top: 4px;">
              <button class="copy-command-btn" data-command="npm run check-ports" style="background: #007bff; color: white; border: none; border-radius: 3px; padding: 4px 8px; font-size: 11px; cursor: pointer; white-space: nowrap;">Copy</button>
              <code style="background: #e9ecef; padding: 2px 6px; border-radius: 3px; flex: 1; font-family: 'Courier New', monospace;">npm run check-ports</code>
            </div>
          </div>
        </div>
        
      </div>
      
      <!-- Footer (not scrollable) -->
      <div class="test-panel-footer" style="margin-top: 10px; padding-top: 10px; border-top: 1px solid #dee2e6; font-size: 11px; color: #6c757d; flex-shrink: 0;">
        Press <kbd class="test-panel-kbd" style="background: #fff; border: 1px solid #ced4da; padding: 2px 6px; border-radius: 3px;">Ctrl+Shift+D</kbd> or click the 🧪 button to toggle
      </div>
    </div> -->

    <!-- <script>
      // Toggle developer test commands panel
      (function() {
        const panel = document.getElementById('dev-test-commands');
        const closeButton = document.getElementById('close-test-panel');
        let isVisible = false;
        
        // Function to toggle panel visibility
        function togglePanel() {
          isVisible = !isVisible;
          panel.style.display = isVisible ? 'flex' : 'none';
        }
        
        // Expose global function for React components to use
        window.toggleDevPanel = function() {
          togglePanel();
        };
        
        // Show in development mode by default (localhost or 127.0.0.1)
        if (window.location.hostname === 'localhost' || window.location.hostname === '127.0.0.1' || window.location.hostname === '') {
          isVisible = false; // Start hidden, user can toggle with Ctrl+Shift+D
          panel.style.display = 'none';
        } else {
          // Hide completely in production
          panel.style.display = 'none';
        }
        
        // Close button click handler
        closeButton.addEventListener('click', togglePanel);
        
        // Check if dark mode is enabled
        function isDarkMode() {
          return window.matchMedia && window.matchMedia('(prefers-color-scheme: dark)').matches;
        }
        
        // Listen for dark mode changes and update styles dynamically
        if (window.matchMedia) {
          const darkModeQuery = window.matchMedia('(prefers-color-scheme: dark)');
          darkModeQuery.addEventListener('change', function(e) {
            // Styles will automatically update via CSS media queries
            // But we can force a re-render if needed
            panel.style.display = panel.style.display; // Force reflow
          });
        }
        
        // Make copy buttons functional
        function copyToClipboard(text, button) {
          navigator.clipboard.writeText(text).then(function() {
            const originalText = button.textContent;
            const darkMode = isDarkMode();
            button.textContent = 'Copied!';
            button.style.background = '#28a745';
            setTimeout(function() {
              button.textContent = originalText;
              button.style.background = darkMode ? '#1976d2' : '#007bff';
            }, 1500);
          }).catch(function(err) {
            console.error('Failed to copy:', err);
            button.textContent = 'Error';
            setTimeout(function() {
              button.textContent = 'Copy';
            }, 1500);
          });
        }
        
        // Attach copy functionality to all copy buttons
        panel.querySelectorAll('.copy-command-btn').forEach(function(button) {
          const command = button.getAttribute('data-command');
          button.addEventListener('click', function(e) {
            e.stopPropagation(); // Prevent panel drag when clicking button
            copyToClipboard(command, button);
          });
          
          // Add hover effect
          button.addEventListener('mouseenter', function() {
            if (this.textContent === 'Copy') {
              this.style.background = isDarkMode() ? '#1565c0' : '#0056b3';
            }
          });
          button.addEventListener('mouseleave', function() {
            if (this.textContent === 'Copy') {
              this.style.background = isDarkMode() ? '#1976d2' : '#007bff';
            }
          });
        });
        
        // Also keep code elements copyable (users can click code or button)
        panel.querySelectorAll('code').forEach(function(code) {
          code.style.cursor = 'pointer';
          code.title = 'Click to copy';
          code.addEventListener('click', function(e) {
            e.stopPropagation(); // Prevent panel drag when clicking code
            const text = this.textContent.trim();
            // Find the corresponding button to show feedback
            const button = this.parentElement.querySelector('.copy-command-btn');
            if (button) {
              copyToClipboard(text, button);
            } else {
              copyToClipboard(text, this);
            }
          });
        });
        
        // Toggle with Ctrl+Shift+D
        document.addEventListener('keydown', function(e) {
          if (e.ctrlKey && e.shiftKey && e.key === 'D') {
            e.preventDefault();
            togglePanel();
          }
        });
        
        // Make panel draggable
        let isDragging = false;
        let currentX, currentY, initialX, initialY;
        
        panel.addEventListener('mousedown', function(e) {
          // Don't start dragging if clicking on code, buttons, or links
          if (e.target.tagName === 'CODE' || 
              e.target.tagName === 'BUTTON' || 
              e.target.tagName === 'KBD' ||
              e.target.closest('button')) {
            return;
          }
          initialX = e.clientX - panel.offsetLeft;
          initialY = e.clientY - panel.offsetTop;
          if (e.target === panel || (panel.contains(e.target) && e.target.tagName !== 'BUTTON')) {
            isDragging = true;
          }
        });
        
        document.addEventListener('mousemove', function(e) {
          if (isDragging) {
            e.preventDefault();
            currentX = e.clientX - initialX;
            currentY = e.clientY - initialY;
            panel.style.left = currentX + 'px';
            panel.style.right = 'auto';
            panel.style.top = currentY + 'px';
            panel.style.bottom = 'auto';
          }
        });
        
        document.addEventListener('mouseup', function() {
          isDragging = false;
        });
      })();
    </script> -->

    <!--
      This HTML file is a template.
      If you open it directly in the browser, you will see an empty page.

      You can add webfonts, meta tags, or analytics to this file.
      The build step will place the bundled scripts into the <body> tag.

      To begin the development, run `npm start` or `yarn start`.
      To create a production bundle, use `npm run build` or `yarn build`.
    -->
  </body>
</html>
